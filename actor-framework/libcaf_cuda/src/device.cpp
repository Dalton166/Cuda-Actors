#include "caf/cuda/device.hpp"
#include <stdexcept>

/*
namespace caf {
namespace cuda {

device::device(cl_device_id, cl_context, cl_command_queue) {
  // Constructor stub: no OpenCL initialization
}

device::~device() {
  // Destructor stub: nothing to release
}

std::string device::vendor() const {
  throw std::runtime_error("OpenCL support disabled: device::vendor()");
}

cl_device_type device::type() const {
  throw std::runtime_error("OpenCL support disabled: device::type()");
}

size_t device::global_mem_size() const {
  throw std::runtime_error("OpenCL support disabled: device::global_mem_size()");
}

size_t device::max_mem_alloc_size() const {
  throw std::runtime_error("OpenCL support disabled: device::max_mem_alloc_size()");
}

size_t device::max_work_group_size() const {
  throw std::runtime_error("OpenCL support disabled: device::max_work_group_size()");
}

} // namespace opencl
} // namespace caf
*/
